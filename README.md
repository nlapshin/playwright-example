# playwright-example
Playwrigth example

### Инструменты

- selenium
- cypress
- puppeteer(очень похож на playwright)

playwright. Headless браузером. Не использует чистые браузеры, а использует их движки.

// npm install

// Инструменты UI тестирования
- умеют запускать браузер и открывать страницы
- уметь взаимодействовать со страницей через API инструмента
- иметь окружения для тестирования(репорты генерить, describe/assert)

Николай Лапшин

Tech Lead, adTech решения.
OTUS JS/node.js/PostreSQL.

Мы можем каждый элемент на странице имеет свой селектор для поиска и свой набор действие(click, fill).

// Для каждого элемента создать действие или список действий.
// Зайти на страницу
// ввести имейл
// ввести пароль
// нажать войти.
// проверить результат.

// Тестируем разные кейсы.

// 1. На каждом сайте есть страницу
// 2. Страницы состоят из элементов.
// 3. У нас есть последовательность действий, которую можно объеденить в процедуру или функции.

// Что такое объект?
// Сущность из свойств(ключи, properties) и методов(функции и прочее).
// Смысл создать удобные интерфейсы для работы с нашими страницами.
// Создать переиспользуемость.
